# SuperMemory System

## Description
A Neo4j + RAG based SuperMemory system. Upload documents, store chunks as memory, retrieve relevant memories, and manage relationships.

## Setup
1. Install dependencies:
   pip install -r requirements.txt

2. Start Neo4j locally or in cloud and update credentials in `memory_manager.py`.

## Run
python main.py
